- name: ansible-vaults
  hosts: local
  connection: local
  vars_files::
  - secrate.yaml
  tasks:
  - name: to view the data 
    ansible.buitin.debud:
      msg: "ADMIN: {{ admin }}, PASSWORD: {{ password }}"