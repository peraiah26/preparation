- name: nginx
  hosts: local
  become: yes
  tasks:
  - name: install the nginx
    ansible.builtin.package:
      name: nginx
      state: present
  - name: start the service
    ansible.builtin.service:
      name: nginx
      state: started
      enabled: yes