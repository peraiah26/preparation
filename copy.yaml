- name: copying files
  hosts: local 
  connection: local
  become: yes
  tasks:
  - name: copying
    ansible.builtin.copy:
      src: 01-ping.yaml
      dest: directory
      owner: ec2-user
      group: ec2-user
      mode: "0655"
